---
interface Props {
    class?: string;
}

import { twMerge } from 'tailwind-merge'


const {class: _class = ""} = Astro.props;
---
<div class:list={[twMerge("grid grid-cols-1 lg:grid-cols-[auto_3fr] items-start ", _class)]}>
    <slot/>
</div>