---
interface Props {
    class?: string;
}

import { twMerge } from 'tailwind-merge';

const { class: _class = '' } = Astro.props;
---

<div class:list={[twMerge('grid grid-cols-1 items-start lg:grid-cols-[400px_3fr] ', _class)]}>
    <slot />
</div>
